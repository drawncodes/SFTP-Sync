# Dependencies
node_modules/

# Build output (regenerated by `npm run compile`)
dist/
out/

# Packaged extension artifacts
*.vsix
_debug/

# Private — local test workspace with real server config & key paths (do NOT publish)
sftp-test/

# Editor / OS / tooling noise
.claude/
.DS_Store
Thumbs.db
*.log
